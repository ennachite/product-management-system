server:
  port: 0

spring:
  application:
    name: product-service
  data:
    mongodb:
      uri: mongodb://admin:password@localhost:27017/products?authSource=admin
      database: products
      username: productUser
      password: password

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka